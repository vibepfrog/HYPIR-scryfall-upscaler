There is intentionally no local BUILD-WINDOWS.bat in v0.4.0.

The supported build path is GitHub Actions, so your PC does not need Python, Git, Conda, PyTorch, HYPIR, PyInstaller, or Inno Setup installed.

See README.md -> "Building the Windows installer".
